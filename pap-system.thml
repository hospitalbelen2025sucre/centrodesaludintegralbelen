<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sistema de Gestión PAP - Centro de Salud Belén</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }
        
        .container {
            max-width: 1200px;
            margin: 0 auto;
            background: white;
            border-radius: 20px;
            box-shadow: 0 20px 60px rgba(0,0,0,0.3);
            overflow: hidden;
        }
        
        .header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 30px;
            text-align: center;
        }
        
        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
        }
        
        .header p {
            font-size: 1.1em;
            opacity: 0.9;
        }
        
        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            padding: 30px;
            background: #f8f9fa;
        }
        
        .stat-card {
            background: white;
            padding: 20px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            text-align: center;
            transition: transform 0.3s ease;
        }
        
        .stat-card:hover {
            transform: translateY(-5px);
        }
        
        .stat-card .icon {
            font-size: 3em;
            margin-bottom: 10px;
        }
        
        .stat-card .number {
            font-size: 2em;
            font-weight: bold;
            color: #667eea;
            margin: 10px 0;
        }
        
        .stat-card .label {
            color: #666;
            font-size: 0.9em;
        }
        
        .content {
            padding: 30px;
        }
        
        .section {
            margin-bottom: 40px;
        }
        
        .section-title {
            font-size: 1.8em;
            color: #333;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 3px solid #667eea;
        }
        
        .form-group {
            margin-bottom: 20px;
        }
        
        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #333;
            font-weight: 500;
        }
        
        .form-group input,
        .form-group select {
            width: 100%;
            padding: 12px;
            border: 2px solid #e0e0e0;
            border-radius: 8px;
            font-size: 1em;
            transition: border-color 0.3s;
        }
        
        .form-group input:focus,
        .form-group select:focus {
            outline: none;
            border-color: #667eea;
        }
        
        .btn {
            padding: 12px 30px;
            border: none;
            border-radius: 8px;
            font-size: 1em;
            cursor: pointer;
            transition: all 0.3s ease;
            font-weight: 600;
        }
        
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
        }
        
        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }
        
        .btn-success {
            background: #28a745;
            color: white;
        }
        
        .btn-danger {
            background: #dc3545;
            color: white;
        }
        
        .btn-info {
            background: #17a2b8;
            color: white;
        }
        
        .btn-group {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }
        
        .search-result {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            margin-top: 20px;
            border-left: 4px solid #667eea;
        }
        
        .warning {
            background: #fff3cd;
            border-left: 4px solid #ffc107;
            padding: 15px;
            border-radius: 8px;
            margin: 15px 0;
        }
        
        .info-box {
            background: #d1ecf1;
            border-left: 4px solid #17a2b8;
            padding: 15px;
            border-radius: 8px;
            margin: 15px 0;
        }
        
        .progress-bar {
            width: 100%;
            height: 30px;
            background: #e0e0e0;
            border-radius: 15px;
            overflow: hidden;
            margin: 20px 0;
        }
        
        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
            transition: width 0.5s ease;
        }
        
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        
        table, th, td {
            border: 1px solid #ddd;
        }
        
        th, td {
            padding: 12px;
            text-align: left;
        }
        
        th {
            background-color: #667eea;
            color: white;
        }
        
        tr:nth-child(even) {
            background-color: #f2f2f2;
        }
        
        .no-data {
            text-align: center;
            padding: 20px;
            color: #666;
            font-style: italic;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🔍 Sistema de Gestión de Informes de Pruebas PAP</h1>
            <p>Búsqueda, consulta y eliminación de resultados de Papanicolaou</p>
            <p>Capacidad de almacenamiento: 3,000 pacientes</p>
        </div>
        
        <div class="stats-container">
            <div class="stat-card">
                <div class="icon">📊</div>
                <div class="number" id="storedReports">2,347</div>
                <div class="label">Informes Almacenados</div>
            </div>
            <div class="stat-card">
                <div class="icon">💾</div>
                <div class="number" id="totalCapacity">3,000</div>
                <div class="label">Capacidad Total</div>
            </div>
            <div class="stat-card">
                <div class="icon">📈</div>
                <div class="number" id="usagePercentage">78.2%</div>
                <div class="label">Uso del Sistema</div>
            </div>
            <div class="stat-card">
                <div class="icon">✅</div>
                <div class="number" id="availableSpace">653</div>
                <div class="label">Espacio Disponible</div>
            </div>
        </div>
        
        <div class="progress-bar">
            <div class="progress-fill" id="progressFill" style="width: 78.2%;">78.2% Utilizado</div>
        </div>
        
        <div class="content">
            <div class="section">
                <h2 class="section-title">🔎 Buscar Informe PAP</h2>
                <div class="form-group">
                    <label>Nombre del Paciente:</label>
                    <input type="text" id="nombrePaciente" placeholder="Ingrese el nombre completo">
                </div>
                <div class="form-group">
                    <label>Carnet de Identidad:</label>
                    <input type="text" id="ciPaciente" placeholder="Ingrese el CI">
                </div>
                <div class="form-group">
                    <label>Fecha de Examen:</label>
                    <input type="date" id="fechaExamen">
                </div>
                <button class="btn btn-primary" onclick="buscarInforme()">🔍 Buscar Informe</button>
            </div>
            
            <div class="section">
                <h2 class="section-title">📋 Resultado de Búsqueda</h2>
                <div class="search-result" id="resultadoBusqueda">
                    <p>Los resultados aparecerán aquí después de realizar la búsqueda</p>
                </div>
            </div>
            
            <div class="section">
                <h2 class="section-title">📋 Lista de Informes PAP</h2>
                <div id="listaInformes">
                    <table id="tablaInformes">
                        <thead>
                            <tr>
                                <th>Nombre</th>
                                <th>CI</th>
                                <th>Fecha de Examen</th>
                                <th>Resultado</th>
                                <th>Acciones</th>
                            </tr>
                        </thead>
                        <tbody id="cuerpoTabla">
                            <!-- Data will be populated here -->
                        </tbody>
                    </table>
                    <div id="noDataMessage" class="no-data">No hay informes registrados</div>
                </div>
            </div>
            
            <div class="section">
                <h2 class="section-title">🗑️ Eliminar Informe</h2>
                <div class="form-group">
                    <label>CI del Paciente a Eliminar:</label>
                    <input type="text" id="ciEliminar" placeholder="Ingrese el CI del paciente">
                </div>
                <div class="warning">
                    <strong>⚠️ Advertencia:</strong> Esta acción no se puede deshacer
                </div>
                <button class="btn btn-danger" onclick="eliminarInforme()">🗑️ Eliminar Informe</button>
            </div>
            
            <div class="section">
                <h2 class="section-title">📥 Exportar e Importar Base de Datos PAP</h2>
                
                <h3><strong>Descargar Base de Datos Completa</strong></h3>
                <div class="btn-group">
                    <button class="btn btn-success" onclick="descargarExcel()">📊 Descargar Excel - Informes PAP</button>
                </div>
                
                <h3 style="margin-top: 30px;"><strong>Cargar/Actualizar Base de Datos</strong></h3>
                <div class="form-group">
                    <label>📎 Seleccionar Archivo Excel</label>
                    <input type="file" id="archivoExcel" accept=".xlsx,.xls">
                </div>
                <div class="btn-group">
                    <button class="btn btn-info" onclick="importarDatos()">⬆️ Importar Datos PAP</button>
                    <button class="btn btn-primary" onclick="descargarPlantilla()">📋 Descargar Plantilla PAP</button>
                </div>
                
                <div class="info-box" style="margin-top: 20px;">
                    <strong>📊 Estadísticas:</strong> <span id="statsText">2,347 informes almacenados de 3,000 disponibles</span>
                </div>
            </div>
        </div>
    </div>
    
    <script>
        // Sample data for demonstration
        let papReports = [
            { id: 1, nombre: "María López", ci: "12345678", fecha: "2023-05-15", resultado: "Normal", observaciones: "Sin alteraciones detectadas" },
            { id: 2, nombre: "Ana García", ci: "23456789", fecha: "2023-06-20", resultado: "Anormal", observaciones: "Requiere seguimiento" },
            { id: 3, nombre: "Laura Pérez", ci: "34567890", fecha: "2023-07-10", resultado: "Normal", observaciones: "Sin alteraciones detectadas" }
        ];
        
        // Initialize the application
        document.addEventListener('DOMContentLoaded', function() {
            actualizarListaInformes();
            actualizarEstadisticas();
        });
        
        function actualizarEstadisticas() {
            const total = 3000;
            const almacenados = papReports.length;
            const porcentaje = ((almacenados / total) * 100).toFixed(1);
            const disponibles = total - almacenados;
            
            document.getElementById('storedReports').textContent = almacenados.toLocaleString();
            document.getElementById('totalCapacity').textContent = total.toLocaleString();
            document.getElementById('usagePercentage').textContent = porcentaje + '%';
            document.getElementById('availableSpace').textContent = disponibles.toLocaleString();
            document.getElementById('progressFill').style.width = porcentaje + '%';
            document.getElementById('progressFill').textContent = porcentaje + '% Utilizado';
            document.getElementById('statsText').textContent = `${almacenados.toLocaleString()} informes almacenados de ${total.toLocaleString()} disponibles`;
        }
        
        function actualizarListaInformes() {
            const cuerpoTabla = document.getElementById('cuerpoTabla');
            const noDataMessage = document.getElementById('noDataMessage');
            
            // Clear existing data
            cuerpoTabla.innerHTML = '';
            
            if (papReports.length === 0) {
                noDataMessage.style.display = 'block';
                return;
            }
            
            noDataMessage.style.display = 'none';
            
            // Populate table with data
            papReports.forEach(report => {
                const row = document.createElement('tr');
                row.innerHTML = `
                    <td>${report.nombre}</td>
                    <td>${report.ci}</td>
                    <td>${report.fecha}</td>
                    <td>${report.resultado}</td>
                    <td>
                        <button class="btn btn-primary" onclick="verDetalle(${report.id})">Ver</button>
                        <button class="btn btn-danger" onclick="eliminarInformePorId(${report.id})">Eliminar</button>
                    </td>
                `;
                cuerpoTabla.appendChild(row);
            });
        }
        
        function buscarInforme() {
            const nombre = document.getElementById('nombrePaciente').value.trim();
            const ci = document.getElementById('ciPaciente').value.trim();
            const fecha = document.getElementById('fechaExamen').value;
            
            if (!nombre && !ci && !fecha) {
                alert('Por favor ingrese al menos un criterio de búsqueda');
                return;
            }
            
            // Filter reports based on search criteria
            const resultados = papReports.filter(report => {
                return (
                    (nombre === '' || report.nombre.toLowerCase().includes(nombre.toLowerCase())) &&
                    (ci === '' || report.ci.includes(ci)) &&
                    (fecha === '' || report.fecha === fecha)
                );
            });
            
            if (resultados.length === 0) {
                document.getElementById('resultadoBusqueda').innerHTML = `
                    <p>No se encontraron resultados para los criterios de búsqueda proporcionados.</p>
                `;
                return;
            }
            
            // Display results
            let resultadosHTML = '<h3>Resultados encontrados:</h3>';
            resultados.forEach(report => {
                resultadosHTML += `
                    <div style="margin-bottom: 15px; padding: 10px; border: 1px solid #ddd; border-radius: 5px;">
                        <p><strong>Nombre:</strong> ${report.nombre}</p>
                        <p><strong>CI:</strong> ${report.ci}</p>
                        <p><strong>Fecha de Examen:</strong> ${report.fecha}</p>
                        <p><strong>Resultado:</strong> ${report.resultado}</p>
                        <p><strong>Observaciones:</strong> ${report.observaciones}</p>
                        <button class="btn btn-danger" onclick="eliminarInformePorId(${report.id})">Eliminar</button>
                    </div>
                `;
            });
            
            document.getElementById('resultadoBusqueda').innerHTML = resultadosHTML;
        }
        
        function verDetalle(id) {
            const report = papReports.find(r => r.id === id);
            if (report) {
                document.getElementById('resultadoBusqueda').innerHTML = `
                    <h3>Detalle del Informe</h3>
                    <p><strong>Nombre:</strong> ${report.nombre}</p>
                    <p><strong>CI:</strong> ${report.ci}</p>
                    <p><strong>Fecha de Examen:</strong> ${report.fecha}</p>
                    <p><strong>Resultado:</strong> ${report.resultado}</p>
                    <p><strong>Observaciones:</strong> ${report.observaciones}</p>
                `;
            }
        }
        
        function eliminarInforme() {
            const ci = document.getElementById('ciEliminar').value.trim();
            
            if (!ci) {
                alert('Por favor ingrese el CI del paciente');
                return;
            }
            
            const index = papReports.findIndex(report => report.ci === ci);
            
            if (index === -1) {
                alert('No se encontró ningún informe con ese CI');
                return;
            }
            
            if (confirm('¿Está seguro de eliminar este informe? Esta acción no se puede deshacer.')) {
                papReports.splice(index, 1);
                alert('Informe eliminado exitosamente');
                document.getElementById('ciEliminar').value = '';
                actualizarListaInformes();
                actualizarEstadisticas();
            }
        }
        
        function eliminarInformePorId(id) {
            const index = papReports.findIndex(report => report.id === id);
            
            if (index === -1) {
                alert('No se encontró el informe');
                return;
            }
            
            if (confirm('¿Está seguro de eliminar este informe? Esta acción no se puede deshacer.')) {
                papReports.splice(index, 1);
                alert('Informe eliminado exitosamente');
                actualizarListaInformes();
                actualizarEstadisticas();
                
                // Clear search results
                document.getElementById('resultadoBusqueda').innerHTML = '<p>Los resultados aparecerán aquí después de realizar la búsqueda</p>';
            }
        }
        
        function descargarExcel() {
            alert('Funcionalidad de descarga de Excel no implementada en esta demostración.\nEn una implementación real, esto generaría y descargaría un archivo Excel con todos los informes PAP.');
        }
        
        function importarDatos() {
            const archivo = document.getElementById('archivoExcel').files[0];
            
            if (!archivo) {
                alert('Por favor seleccione un archivo Excel');
                return;
            }
            
            alert('Funcionalidad de importación no implementada en esta demostración.\nEn una implementación real, esto procesaría el archivo Excel y actualizaría la base de datos PAP.');
        }
        
        function descargarPlantilla() {
            alert('Funcionalidad de descarga de plantilla no implementada en esta demostración.\nEn una implementación real, esto descargaría una plantilla Excel para importar datos PAP.');
        }
    </script>
</body>
</html>
